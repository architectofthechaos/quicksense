# SPDX-License-Identifier: Apache-2.0
# Polaris — bootstrap Job + Deployment + NodePort Service (8181→30181, 8182 ClusterIP)
# Mirrors docker/docker-compose.yml services `polaris-bootstrap` and `polaris`.
#
# Bootstrap Job entrypoint decision:
#   The polaris-admin-tool image sets its own entrypoint that invokes the tool
#   binary directly. We override command/args to wrap with /bin/sh so that exit
#   code 3 ("already bootstrapped") is treated as success. This means the Job
#   tolerates re-runs (e.g. after cluster recreate with a pre-seeded PVC or
#   when the postgres-data PVC already has the realm).  On a fresh PVC the tool
#   exits 0; on an already-bootstrapped DB it exits 3, which the shell converts
#   to 0.  backoffLimit:6 handles transient postgres-not-ready failures.
#
# KNOWN RISK — dotted env key names:
#   Keys such as `polaris.features."ALLOW_INSECURE_STORAGE_TYPES"` contain dots
#   and quotes that are not valid POSIX shell variable names.  They are written
#   here verbatim because Quarkus reads them directly from the JVM system
#   properties / quarkus env source, not through the shell environment.
#   Whether `kubectl apply` accepts these strings as env[].name values is only
#   confirmed at live apply time (Task A12).  If the apiserver rejects them,
#   the workaround is to set them via a Quarkus application.properties ConfigMap
#   volume-mounted at /deployments/config/application.properties.
---
apiVersion: batch/v1
kind: Job
metadata:
  name: polaris-bootstrap
spec:
  backoffLimit: 6
  ttlSecondsAfterFinished: 600
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: polaris-bootstrap
          image: apache/polaris-admin-tool:1.5.0
          command:
            - /bin/sh
            - -c
          args:
            - >-
              java -jar /deployments/polaris-admin-tool.jar bootstrap
              --realm=$(POLARIS_REALM)
              --credential=$(POLARIS_REALM),$(POLARIS_CLIENT_ID),$(POLARIS_CLIENT_SECRET)
              || [ $? -eq 3 ]
          envFrom:
            - configMapRef:
                name: qs-config
            - secretRef:
                name: qs-secrets
          env:
            - name: POLARIS_PERSISTENCE_TYPE
              value: relational-jdbc
            - name: POLARIS_PERSISTENCE_RELATIONAL_JDBC_DATABASE_TYPE
              value: postgresql
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: "jdbc:postgresql://postgres:5432/$(POSTGRES_DB)"
            - name: QUARKUS_DATASOURCE_USERNAME
              value: "$(POSTGRES_USER)"
            - name: QUARKUS_DATASOURCE_PASSWORD
              value: "$(POSTGRES_PASSWORD)"
---
apiVersion: v1
kind: Service
metadata:
  name: polaris
spec:
  type: NodePort
  selector:
    app: polaris
  ports:
    - name: catalog
      port: 8181
      targetPort: 8181
      nodePort: 30181
    - name: management
      port: 8182
      targetPort: 8182
      # No nodePort — reached in-cluster and via kubectl port-forward
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: polaris
spec:
  replicas: 1
  selector:
    matchLabels:
      app: polaris
  template:
    metadata:
      labels:
        app: polaris
    spec:
      containers:
        - name: polaris
          image: apache/polaris:1.5.0
          ports:
            - containerPort: 8181
            - containerPort: 8182
          envFrom:
            - configMapRef:
                name: qs-config
            - secretRef:
                name: qs-secrets
          env:
            - name: POLARIS_PERSISTENCE_TYPE
              value: relational-jdbc
            - name: POLARIS_PERSISTENCE_RELATIONAL_JDBC_DATABASE_TYPE
              value: postgresql
            - name: POLARIS_PERSISTENCE_RELATIONAL_JDBC_MAX_RETRIES
              value: "10"
            - name: POLARIS_PERSISTENCE_RELATIONAL_JDBC_INITIAL_DELAY_IN_MS
              value: "250"
            - name: POLARIS_PERSISTENCE_RELATIONAL_JDBC_MAX_DURATION_IN_MS
              value: "10000"
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: "jdbc:postgresql://postgres:5432/$(POSTGRES_DB)"
            - name: QUARKUS_DATASOURCE_USERNAME
              value: "$(POSTGRES_USER)"
            - name: QUARKUS_DATASOURCE_PASSWORD
              value: "$(POSTGRES_PASSWORD)"
            - name: POLARIS_REALM_CONTEXT_REALMS
              value: "$(POLARIS_REALM)"
            - name: POLARIS_BOOTSTRAP_CREDENTIALS
              value: "$(POLARIS_REALM),$(POLARIS_CLIENT_ID),$(POLARIS_CLIENT_SECRET)"
            - name: QUARKUS_OTEL_SDK_DISABLED
              value: "true"
            - name: AWS_REGION
              value: us-east-1
            - name: AWS_ACCESS_KEY_ID
              value: "$(MINIO_ROOT_USER)"
            - name: AWS_SECRET_ACCESS_KEY
              value: "$(MINIO_ROOT_PASSWORD)"
            # Auth: Polaris 1.5 supports MIXED mode (enum INTERNAL/EXTERNAL/MIXED) — live-verified.
            # Mixed mode accepts BOTH the internal root:s3cr3t credential AND Keycloak JWTs
            # simultaneously. A previous commit incorrectly set this to "internal"; this is
            # the correct production-validated value.
            #
            # Principal resolution: polaris.oidc.principal-mapper.name-claim-path=preferred_username
            # is REQUIRED. Without it Polaris cannot extract a principal from the JWT and returns
            # 401 "Failed to resolve principal". It resolves by NAME to a Polaris principal
            # (service-account-quicksense-api) created by ensure_polaris_external_principal in
            # bootstrap. Do NOT set id-claim-path=sub — Polaris parses the id as a numeric long
            # and a UUID sub throws NumberFormatException (400).
            #
            # The ^polaris_(.*) mapper turns Keycloak realm role `polaris_admin` into
            # Polaris principal role `admin` (PRINCIPAL_ROLE:admin), bound to catalog_admin
            # on the quicksense catalog by ensure_polaris_admin_principal_role in bootstrap.
            #
            # Live-verified: Keycloak JWT → list-namespaces on quicksense catalog → 200.
            - { name: quarkus.oidc.tenant-enabled, value: "true" }
            - { name: quarkus.oidc.auth-server-url, value: "http://keycloak:8082/realms/quicksense" }
            - { name: quarkus.oidc.client-id, value: "quicksense-api" }
            - { name: quarkus.oidc.roles.role-claim-path, value: "realm_access/roles" }
            - { name: polaris.oidc.principal-roles-mapper.type, value: "default" }
            - { name: "polaris.oidc.principal-roles-mapper.mappings[0].regex", value: "^polaris_(.*)" }
            - { name: "polaris.oidc.principal-roles-mapper.mappings[0].replacement", value: "PRINCIPAL_ROLE:$1" }
            - { name: polaris.oidc.principal-mapper.name-claim-path, value: "preferred_username" }
            - { name: polaris.authentication.type, value: "mixed" }
            # Dotted Quarkus feature-flag keys — see KNOWN RISK note at top of file.
            - name: 'polaris.features."ALLOW_INSECURE_STORAGE_TYPES"'
              value: "true"
            - name: 'polaris.features."ALLOW_SETTING_S3_ENDPOINTS"'
              value: "true"
            - name: 'polaris.features."SUPPORTED_CATALOG_STORAGE_TYPES"'
              value: '["FILE","S3","GCS","AZURE"]'
            - name: polaris.readiness.ignore-severe-issues
              value: "true"
          readinessProbe:
            httpGet:
              path: /q/health
              port: 8182
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /q/health
              port: 8182
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
