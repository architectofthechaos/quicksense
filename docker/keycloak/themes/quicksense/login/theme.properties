# QuickSense login theme — drop-in over the stock Keycloak image.
# Inherits the base `keycloak` login theme (templates, messages, and all flows:
# MFA, forgot-password, registration) and overrides only the styling + logo.
parent=keycloak
import=common/keycloak
styles=css/login.css
